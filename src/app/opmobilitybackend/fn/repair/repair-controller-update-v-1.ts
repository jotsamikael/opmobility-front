/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Repair } from '../../models/repair';
import { UpdateRepairDto } from '../../models/update-repair-dto';

export interface RepairControllerUpdateV1$Params {
  id: number;
      body: UpdateRepairDto
}

export function repairControllerUpdateV1(http: HttpClient, rootUrl: string, params: RepairControllerUpdateV1$Params, context?: HttpContext): Observable<StrictHttpResponse<Repair>> {
  const rb = new RequestBuilder(rootUrl, repairControllerUpdateV1.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'multipart/form-data');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Repair>;
    })
  );
}

repairControllerUpdateV1.PATH = '/api/v1/repair/{id}';
