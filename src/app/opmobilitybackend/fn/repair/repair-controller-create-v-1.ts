/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateRepairDto } from '../../models/create-repair-dto';
import { Repair } from '../../models/repair';

export interface RepairControllerCreateV1$Params {
      body: CreateRepairDto
}

export function repairControllerCreateV1(http: HttpClient, rootUrl: string, params: RepairControllerCreateV1$Params, context?: HttpContext): Observable<StrictHttpResponse<Repair>> {
  const rb = new RequestBuilder(rootUrl, repairControllerCreateV1.PATH, 'post');
  if (params) {
    rb.body(params.body, 'multipart/form-data');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Repair>;
    })
  );
}

repairControllerCreateV1.PATH = '/api/v1/repair';
